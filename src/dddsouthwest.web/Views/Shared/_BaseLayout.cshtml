@using DDDSouthWest.Web.Framework

<!DOCTYPE html>
<html lang="en">
<head>
    @if (!string.IsNullOrEmpty(ViewBag.TitleTag))
    {
      <title>@ViewBag.TitleTag</title>
    }

    @if (!string.IsNullOrEmpty(ViewBag.MetaDescription))
    {
        <meta name="description" content="@ViewBag.MetaDescription">
    }

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700,800" rel="stylesheet">

	<environment names="Development">
        <link href="/css/normalize.css" rel="stylesheet" />
        <link href="/css/unsemantic-grid-responsive-no-ie7.css" rel="stylesheet" />
	    <link href="/css/default.css" rel="stylesheet" />
	    <link href="/css/layout.css" rel="stylesheet" />
	    <link href="/css/nav.css" rel="stylesheet" />
    </environment>    
    
    @if (IsSectionDefined("Css"))
    {
        @RenderSection("Css", false)
    }

    @if (IsSectionDefined("ScriptsHeader"))
    {
        @RenderSection("ScriptsHeader", false)
    }
</head>
<body>
        
<header>
    
    <div class="header-container">
        
        <a href="/" class="site-logo">
            <span class="site-logo__title">DDD South West</span>
        </a>
        <nav class="main-menu-container">
            <ul class="main-menu">
                <li @Html.Raw(ViewBag.Tab == PageTab.Home ? "class=\"active\"" : string.Empty)><a href="/">Home</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.About ? "class=\"active\"" : string.Empty)><a href="/page/about">About</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.Speakers ? "class=\"active\"" : string.Empty)><a href="/page/speakers">Speakers</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.Sessions ? "class=\"active\"" : string.Empty)><a href="/page/speakers">Sessions</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.Venue ? "class=\"active\"" : string.Empty)><a href="/page/venue">Venue</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.Sponsors ? "class=\"active\"" : string.Empty)><a href="/page/sponsors">Sponsors</a></li>
                <li @Html.Raw(ViewBag.Tab == PageTab.Contact ? "class=\"active\"" : string.Empty)><a href="/page/contact">Contact</a></li>
            </ul>
        </nav>
        
        <div class="clear"></div>
    </div>

</header>
    


@RenderBody()

@if (IsSectionDefined("ScriptsFooter"))
{
    @RenderSection("ScriptsFooter", false)
}

</body>
</html>